#include <iostream>
#include <string>

enum color_t { WHITE, BLACK, RED };
enum class Color { white, black, red };
enum class Shape : unsigned int;
enum class ShapeChar : char
{
    CIRCLE = 'a', RECTANGLE = 'b'
};

int main()
{

}